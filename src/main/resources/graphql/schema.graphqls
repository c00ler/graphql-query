type Query {
    users: [User]
    user(id: ID): User
}

type User {
    id: ID
    firstName: String
    lastName: String
    address: Address
}

type Address {
    addressLine: String
    city: String
    zipCode: String
}
